# 📄 PR0301: Condicional `if`

## 📌 Ejercicio 1: Contar líneas de un archivo
Creamos un script con el nombre **pr0301_1.sh** con `touch "archivo"` y le ponemos los permisos con `sudo chmod +x "archivo"` para ejecutarlo sin problemas. Los demás scripts tendrán la misma estructura del nombre pero con el número del ejercicio para diferenciarlo de los demás y no equivocarnos al editar o ejecutar los demás scripts.

- Script:
```
#!/bin/bash
read -p "Nombre del archivo: " archivo
if [ -f "$archivo" ]; then
  lineas=$(wc -l < "$archivo")
  echo "El archivo '$archivo' tiene $lineas líneas."
else
  echo "Error: el '$archivo' no existe o está mal escrito."
  exit 1
fi
```

- Resultado:

![Script 1](Imagenes/1.png)

## 📌 Ejercicio 2: Verificación de archivo
Creamos un script con el nombre **pr0301_2.sh**.

- Script:
```
#!/bin/bash
read -p "Ruta del archivo: " ruta
if [ -e "$ruta" ]; then
  if [ -f "$ruta" ]; then
    if [ -r "$ruta" ]; then
      echo "El archivo '$ruta' existe y tiene permisos de lectura."
    else
      echo "El archivo '$ruta' existe pero NO tiene permisos de lectura."
    fi
  else
    echo "La ruta '$ruta' existe pero no es un archivo corriente."
  fi
else
  echo "La ruta '$ruta' NO existe."
fi
```

- Resultado:

![Script 2](Imagenes/2.png)

## 📌 Ejercicio 3: Comparación de dos números
Creamos un script con el nombre **pr0301_3.sh**.

- Script:
```
#!/bin/bash
read -p "Introduce el primer número: " a
read -p "Introduce el segundo número: " b
if ! [[ "$a" =~ ^-?[0-9]+$ ]] || ! [[ "$b" =~ ^-?[0-9]+$ ]]; then
  echo "Error: introduce números enteros."
  exit 1
fi

if [ "$a" -gt "$b" ]; then
  echo "$a es mayor que $b."
elif [ "$a" -lt "$b" ]; then
  echo "$b es mayor que $a."
else
  echo "Ambos números son iguales ($a)."
fi
```

- Resultado:

![Script 3](Imagenes/3.png)

## 📌 Ejercicio 4: Validación de contraseña
Creamos un script con el nombre **pr0301_4.sh**.

- Script:
```
#!/bin/bash
contrasena="1234"
read -s -p "Introduce la contraseña: " entrada
echo ""
if [ "$entrada" = "$contrasena" ]; then
  echo "Contraseña correcta. Acceso concedido."
else
  echo "Contraseña incorrecta. Acceso denegado."
fi
```

- Resultado:

![Script 4](Imagenes/4.png)

## 📌 Ejercicio 5: Comprobación de directorio
Creamos un script con el nombre **pr0301_5.sh**.

- Script:
```
#!/bin/bash
read -p "Introduce la ruta del directorio: " dir
if [ -d "$dir" ]; then
  if [ -w "$dir" ]; then
    echo "El directorio '$dir' existe y tiene permisos de escritura."
  else
    echo "El directorio '$dir' existe pero NO tiene permisos de escritura."
  fi
else
  echo "El directorio '$dir' no existe. Creando..."
  if mkdir -p "$dir"; then
    echo "Directorio creado: $dir"
  else
    echo "Error al crear el directorio $dir."
    exit 1
  fi
fi
```

- Resultado:

![Script 5](Imagenes/5.png)

## 📌 Ejercicio 6: Verificar si el usuario es `root`
Creamos un script con el nombre **pr0301_6.sh**.

- Script:
```
#!/bin/bash
if [ "$(id -u)" -eq 0 ]; then
  echo "Estás ejecutando el script como root."
else
  echo "No eres root. Ejecuta el script como root para privilegios completos."
fi
```

- Resultado:

![Script 6](Imagenes/6.png)

## 📌 Ejercicio 7: Calificación de un examen
Creamos un script con el nombre **pr0301_7.sh**.

- Script:
```
#!/bin/bash
read -p "Introduce la nota (0-10): " nota
if ! [[ "$nota" =~ ^[0-9]+([.][0-9]+)?$ ]]; then
  echo "Nota inválida."
  exit 1
fi

aprobado=$(echo "$nota >= 5" | bc -l)
if [ "$aprobado" -eq 1 ]; then
  echo "Aprobado (nota: $nota)."
else
  echo "Suspenso (nota: $nota)."
fi
```

- Resultado:

![Script 7](Imagenes/7.png)

## 📌 Ejercicio 8: Comprobación del espacio en disco
Creamos un script con el nombre **pr0301_8.sh**.

- Script:
```
#!/bin/bash
usado=$(df --output=pcent / | tail -1 | tr -dc '0-9')
if [ -z "$usado" ]; then
  echo "No se pudo obtener el uso del disco."
  exit 1
fi

libre=$((100 - usado))
echo "Espacio libre en /: ${libre}%"
if [ "$libre" -lt 10 ]; then
  echo "ADVERTENCIA: Espacio libre inferior al 10%!"
else
  echo "Espacio suficiente."
fi
```

- Resultado:

![Script 8](Imagenes/8.png)

## 📌 Ejercicio 9: Menú de opciones
Creamos un script con el nombre **pr0301_9.sh**.

- Script:
```
#!/bin/bash
echo "----------------------------------------"
echo "1) Mostrar saludo"
echo "2) Mostrar fecha actual"
echo "3) Salir"
echo "----------------------------------------"
read -p "Elige una opción (1-3): " opcion
if [ "$opcion" -eq 1 ]; then
  echo "¡Hola! Espero que tengas un gran día"
elif [ "$opcion" -eq 2 ]; then
  echo "La fecha y hora actual es: $(date)"
elif [ "$opcion" -eq 3 ]; then
  echo "Saliendo del programa. ¡Hasta pronto!"
else
  echo "Opción no válida. Por favor elige 1, 2 o 3."
fi
```

- Resultado:

![Script 9](Imagenes/9.png)

## 📌 Ejercicio 10: Evaluación de edad
Creamos un script con el nombre **pr0301_10.sh**.

- Script:
```
#!/bin/bash
read -p "Introduce tu edad: " edad
if ! [[ "$edad" =~ ^[0-9]+$ ]]; then
  echo "Edad incorrecta."
  exit 1
fi

if [ "$edad" -lt 18 ]; then
  echo "Eres menor de edad."
elif [ "$edad" -le 65 ]; then
  echo "Eres adulto."
else
  echo "Eres mayor de 65."
fi
```

- Resultado:

![Script 10](Imagenes/10.png)

---
### [⬅️ Volver a UT03](../index.md)
---